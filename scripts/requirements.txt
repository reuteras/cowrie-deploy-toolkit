# Cowrie Daily Report Requirements

# HTTP requests (VirusTotal, webhooks, SendGrid/Mailgun)
requests>=2.31.0

# GeoIP lookups (MaxMind GeoLite2)
geoip2>=4.7.0

# YARA malware scanning
yara-python>=4.3.1

# Email sending (standard library, no install needed for smtplib)
# But we'll add these for completeness
