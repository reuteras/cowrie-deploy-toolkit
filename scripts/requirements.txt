# Cowrie Daily Report Requirements
#
# DEPRECATED: This file is kept for backward compatibility only.
# Please use pyproject.toml and uv for dependency management instead.
#
# Installation with uv (recommended):
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   uv sync
#
# Installation with pip (legacy):
#   pip3 install -r requirements.txt
#

# HTTP requests (VirusTotal, webhooks, SendGrid/Mailgun)
requests>=2.31.0

# GeoIP lookups (MaxMind GeoLite2)
geoip2>=4.7.0

# YARA malware scanning
yara-python>=4.3.1
